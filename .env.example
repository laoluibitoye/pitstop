# Supabase Configuration (Required - NEVER commit actual keys)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-super-secure-secret-key-min-32-chars

# Development Environment
NODE_ENV=development

# Optional: For production deployment
# VERCEL_URL=your-app.vercel.app
# DATABASE_URL=your-database-url-if-using-different-db

# Security Notes:
# 1. NEVER commit actual API keys to version control
# 2. Use different keys for development and production
# 3. Rotate keys regularly
# 4. Use environment-specific configurations
# 5. Enable Supabase Row Level Security (RLS)
# 6. Implement proper authentication and authorization